
var $ = require('jquery')

process.stdout.write("GNU is Not Linux")
process.stdout.write("Requires but doesn't use jQuery.")

Math.min(50, 25, 10, 0, -5, -1);      // retorna -5
Math.max(50, 25, 10, 0, -5, -1);      // retorna 50
